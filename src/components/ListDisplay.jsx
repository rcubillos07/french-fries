import { useState } from "react";

//check .map syntax, not working properly

export default function ListDisplay({TaskList, setTaskList}){
    return(
        <div>
            <ul>
            
            </ul>
        </div>
    )
}